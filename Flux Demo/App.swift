import SwiftUI

@main
struct FluxKontextDemoApp: App {
  var body: some Scene {
    WindowGroup {
      ContentView()
        .frame(minWidth: 800, minHeight: 600)
    }
    .defaultSize(width: 800, height: 600)
  }
}
